//1 def odd_numbers(start, end):
    for number in range(start, end + 1):
        if number % 2 != 0:
            yield number
//2 def outside_range(numbers, start, end):
    for number in numbers:
        if number < start or number > end:
            yield number
